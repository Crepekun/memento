

document.addEventListener('DOMContentLoaded', () => {
    <!-- r, rr, sr, srr, ssr -->
    <!-- 72, 24, 8, 4, 1 -->
    function rarity_calculation(r, rr, sr, srr, rarity) {
        const count_value = document.getElementById('count').value = Math.round(1);
        const necessary_rarity_value = document.getElementById('necessary_rarity').value = Math.round(1);
        const necessary_diamond_value = document.getElementById('necessary_diamond').value = Math.round(1);
    }
